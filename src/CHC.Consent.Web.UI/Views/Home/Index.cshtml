@using System.Security.Claims

Hello @User.Identity.Name

<dl>
    @foreach (var claim in User.Claims)
    {
        <dt>@claim.Type</dt>
        <dd>@claim.Value</dd>
    }
</dl>

@section Scripts
{
    <script>
        
    </script>
}
